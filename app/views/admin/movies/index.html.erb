<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理者用 映画一覧ページ</title>
  </head>
  <body>
    <div class="flex justify-between mx-4 items-center py-4">
      <h1 class="text-4xl font-bold">管理者用 映画一覧ページ</h1>
      <%= link_to  "追加", new_admin_movie_path %>
    </div>
    <div class="mb-4">
      <%= label_tag :keyword, "キーワード検索" %>
      <%= text_field_tag :keyword, params[:keyword], class: "border rounded px-2 py-1" %>
    </div>
    <div class="mb-4">
      <%= label_tag :is_showing, "公開状態" %> <br>
      <%= radio_button_tag :is_showing, %>
    </div>
    <!-- 編集中..... -->
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>タイトル</th>
          <th>公開年</th>
          <th>概要</th>
          <th>画像URL</th>
          <th>上映中</th>
          <th>登録日時</th>
          <th>更新日時</th>
          <th>編集</th>
        </tr>
      </thead>
      <tbody>
        <% @movies.each do |movie| %>
          <tr>
            <td><%= movie.id%></td>
            <td><%= movie.name %></td>
            <td><%= movie.year %></td>
            <td><%= movie.description %></td>
            <td><%= link_to  "画像", movie.image_url, target: "_blank" %></td>
            <td><%= movie.is_showing ? "上映中" : "上映予定" %></td>
            <td><%= movie.created_at %></td>
            <td><%= movie.updated_at %></td>
            <td><%= link_to  "編集", edit_admin_movie_path(movie) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>
